FORGEENGINE ICON ASSETS – PNG (fallback)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Same file names as SVG directory, but .png extension.
Size: 64×64 pixels, RGBA (transparent background, white icon).
Only used when SVG fails to load.
